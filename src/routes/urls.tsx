export enum urls {
    BASE_URL = "/",
    LOGIN = "/login",
    CATERGORY = "/category",
    USERS = "/users",
    CUSTOMERS = "/customers",
    Packages = "/packages",
    SLOTS = "/slots",
    STATE = "/state",
    CITIES = "cities/:id",
}